# This file is generated by GitLab CI
pylint:
  script:
  - virtualenv venv --quiet
  - source venv/bin/activate
  - pip install -r requirements.txt --quiet
  - pip install pylint --quiet
  - pylint --output-format=colorized  --rcfile=.pylintrc maxskins
